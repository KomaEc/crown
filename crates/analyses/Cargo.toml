[package]
name = "analyses"
version = "0.1.0"
edition = "2024"

[package.metadata.rust-analyzer]
rustc_private = true

[dependencies]
utils = { path = "../utils" }

[dev-dependencies]
utils = { path = "../utils", features = ["test-utils"] }
